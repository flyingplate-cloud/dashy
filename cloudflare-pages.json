{
  "build": {
    "command": "yarn install --frozen-lockfile=false && NODE_OPTIONS=--openssl-legacy-provider yarn build",
    "output": "dist",
    "environment": {
      "NODE_VERSION": "18"
    }
  },
  "deploy": {
    "command": "npx wrangler deploy"
  }
}